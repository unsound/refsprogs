#!/bin/sh

set -e

case "${1}" in
	prereqs)
		exit 0
		;;
esac

if [ "${ROOTFSTYPE}" = refs ] || [ "${ROOTFSTYPE}" = refs-fuse ] || \
   [ "${LOOPFSTYPE}" = refs ] || [ "${LOOPFSTYPE}" = refs-fuse ]
then
	modprobe fuse
fi

exit 0
