obj-m := refs.o

ccflags-y := \
	-DVERSION=\"$(shell grep AC_INIT ../../configure.ac | cut -d , -f 2 | cut -d '[' -f 2 | cut -d ']' -f 1)\"

refs-y := \
	refs_init.o \
	fsapi_linux.o \
	sys_linux.o \
	fsapi.o \
	node.o \
	rb_tree.o \
	volume.o
